{
  "name": "dwellia-hostbuddy-alert",
  "version": "1.0.0",
  "description": "HostbuddyAI → Claude → Quo SMS + Asana task routing for Dwellia",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.51.0",
    "@neondatabase/serverless": "^0.10.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
